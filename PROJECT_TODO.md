# PROJECT_TODO.md — Birchscents Implementation Roadmap

Frontend-first. Phases 2–5 build the entire storefront against mock data behind the typed
data layer (`lib/api/`); the backend only begins once the UI is finished. Every task is
small enough to complete in one focused session. Work strictly in order within a phase;
check off tasks as they land. Conventions live in `CLAUDE.md` — read it before each phase.

**Brand tagline:** "Inhale and Feel the Difference."

---

## Phase 1 — Project Setup

- [x] 1.1 Initialize git repo with root `.gitignore` (node, PHP, `.env*`, `.next`, `vendor`).
- [x] 1.2 Scaffold `/frontend`: latest Next.js (App Router, TypeScript, Tailwind, ESLint, `src/` dir, import alias `@/`).
- [x] 1.3 Enable TypeScript `strict` mode; add Prettier + a `format` script; verify `next build` passes clean.
- [x] 1.4 Initialize shadcn/ui in `/frontend` (CSS variables mode); install Framer Motion and Zustand.
- [x] 1.5 Create the folder skeleton from CLAUDE.md §2 (`components/{ui,layout,product,cart,checkout,marketing,admin}`, `lib/api`, `lib/supabase`, `mocks`, `stores`, `types`) with placeholder index files.
- [x] 1.6 Create `frontend/.env.example` with the four `NEXT_PUBLIC_*` keys (values blank).
- [ ] 1.7 Create a Supabase project (free tier); note the URL, anon key, JWT secret, and DB connection string somewhere safe (not in git).
- [x] 1.8 Commit: "Phase 1 — project setup".

*(Backend scaffolding is deliberately deferred to Phase 6.)*

## Phase 2 — Design System & Mock Data

- [x] 2.1 Define design tokens in `globals.css` + Tailwind config: warm ivory/cream background, deep charcoal text, one muted gold/amber accent, semantic tokens (background, foreground, muted, accent, border) for the shadcn variables.
- [x] 2.2 Set up typography with `next/font`: a serif display face for headlines, a refined sans for body/UI; define the type scale (display, h1–h4, body, small, overline/eyebrow).
- [x] 2.3 Add and fully restyle core shadcn primitives: Button (solid/outline/ghost), Input, Label, Select, Sheet (cart drawer), Dialog, Badge, Skeleton, Separator, Sonner/Toast. None may look like default shadcn.
- [ ] 2.4 Build a throwaway `/dev/styleguide` page rendering all tokens, type scale, and primitives for visual QA (delete before launch).
- [x] 2.5 Define the API contract types in `src/types/`: `Category`, `Product`, `ProductVariant`, `CartItem`, `Order`, `OrderItem`, `Paginated<T>`, `ApiError` — all money fields as `*Pesewas: number` (integers).
- [x] 2.6 Write `lib/money.ts` with `formatPesewas()` (`Intl.NumberFormat en-GH / GHS`) + unit-style sanity checks.
- [x] 2.7 Create mock data in `src/mocks/`: 4 categories (Reed Diffusers, Room Sprays, Fragrance Oils, Humidifiers), ~12 products with sensory luxury copy (Snow Melon flagged `isFeatured`), 50ml/100ml variants where applicable, realistic pesewa prices, placeholder imagery (elegant neutral-toned placeholders or licensed stock).
- [x] 2.8 Build the mock-backed data layer: `lib/api/products.ts`, `categories.ts` etc. returning typed promises from mocks with a ~300 ms delay. Add `client.ts` as a stub for now.
- [x] 2.9 Create shared motion presets (`lib/motion.ts`): fade-up-on-scroll, stagger container, hover-lift — all with `prefers-reduced-motion` fallbacks.
- [x] 2.10 Commit: "Phase 2 — design system, types, mock data layer".

## Phase 3 — Homepage

- [x] 3.1 Build `Header`: logo, nav (Shop, category links), cart icon with count badge, account icon; transparent-over-hero → solid-on-scroll behavior.
- [x] 3.2 Build `MobileNav`: full-screen or sheet menu, large elegant links, staggered entrance animation; fully keyboard/focus-trap accessible.
- [x] 3.3 Build `Footer`: brand statement, category links, contact (Accra, Ghana), quiet trust line; plus a slim `AnnouncementBar` ("Nationwide delivery across Ghana").
- [x] 3.4 Build the `Hero`: full-bleed premium visual, serif headline, sensory sub-line, single CTA to Shop; subtle entrance animation. Feature the brand tagline "Inhale and Feel the Difference" (as headline or eyebrow).
- [x] 3.5 Build `TrustBar`: FDA Approved · Long-lasting Fragrance · Nationwide Delivery · Premium Support — one quiet horizontal strip with fine icons.
- [x] 3.6 Build `CategoryShowcase`: four large image cards linking to category-filtered shop pages, hover zoom/lift.
- [x] 3.7 Build `SignatureScent` section for Snow Melon: asymmetric editorial layout, the flagship description copy, CTA to its product page.
- [x] 3.8 Build `FeaturedProducts` row reusing `ProductCard` (built in 4.1 — stub it now if needed) fed by `getFeaturedProducts()`.
- [x] 3.9 Build a `ForBusiness` teaser section (hotels, spas, offices — corporate gifting) with a contact CTA (mailto/phone for MVP).
- [x] 3.10 Assemble the landing page from these sections; scroll-triggered reveals; QA at 375/768/1440 px; run Lighthouse and fix anything under target.
- [x] 3.11 Commit: "Phase 3 — homepage".

## Phase 4 — Shop & Product Pages

- [x] 4.1 Build `ProductCard`: large image, name, category eyebrow, from-price (`formatPesewas`), hover lift + secondary image or subtle zoom, whole card clickable with proper link semantics.
- [x] 4.2 Build the Shop page `/shop`: responsive grid (2-col mobile → 4-col desktop), page intro header, `Skeleton` loading state.
- [x] 4.3 Add category filtering: elegant pill/tab bar + `/shop/[category]` routes with correct metadata per category; empty-state design.
- [x] 4.4 Build the PDP `/products/[slug]` layout: gallery left / details right on desktop, stacked on mobile.
- [x] 4.5 Build `ProductGallery`: main image + thumbnails, crossfade transitions, `next/image` throughout.
- [x] 4.6 Build `VariantSelector` (50ml/100ml): accessible radio-group styled as premium size chips; price updates with selection; out-of-stock variants disabled with a tasteful note.
- [x] 4.7 Build PDP details: name, tagline, sensory description, scent notes, quantity stepper, prominent Add-to-Cart button; sticky add-to-cart bar on mobile.
- [x] 4.8 Add a "Complete the atmosphere" related-products row (same category, from mocks).
- [x] 4.9 Add `generateMetadata` for shop + PDP (title, description, OG image) and a `not-found` state for bad slugs.
- [x] 4.10 Responsive + accessibility QA pass on shop and PDP; commit: "Phase 4 — shop & product pages".

## Phase 5 — Cart & Checkout UI

- [x] 5.1 Build the cart store (`stores/cart.ts`): Zustand + `persist` (localStorage); items keyed by `variantId`; add/update-qty/remove/clear; derived `subtotalPesewas` and count. Integers only.
- [x] 5.2 Wire Add-to-Cart on the PDP: adds item, opens the cart drawer, toast confirmation.
- [x] 5.3 Build `CartDrawer` (customized Sheet): line items with thumbnails, variant label, qty steppers, remove; subtotal; "Checkout" and "Continue shopping" CTAs; empty-cart state with a warm invitation back to Shop.
- [x] 5.4 Build the full `/cart` page (same store, roomier layout) for direct visits.
- [x] 5.5 Build the checkout page `/checkout`: order summary (line items + subtotal + flat delivery fee + total) alongside a delivery details form (name, phone, address, city, note) with inline validation.
- [x] 5.6 Add the auth gate UX: unauthenticated users hitting checkout are routed to login with a `redirectTo` back to checkout.
- [x] 5.7 Build login/register pages in the `(auth)` layout: email+password forms and a "Continue with Google" button — pure UI for now (fake success), styled to the brand.
- [x] 5.8 Build the order-confirmation page (`/orders/confirmation/[orderNumber]`): thank-you moment, order number, summary, delivery details — fed by mock order data.
- [x] 5.9 Build `/orders` (customer order history): list with status badges, and an order detail view — mock data.
- [x] 5.10 Build the admin UI shell (`/admin`): sidebar layout + three screens against mocks — products table with create/edit form (name, category, copy, variants, prices in pesewas), inventory view (stock per variant, inline adjust), orders table with status detail/update.
- [x] 5.11 Full storefront walkthrough QA: landing → shop → PDP → cart → checkout → confirmation on mobile + desktop; fix rough edges; delete-or-polish pass on animations.
- [x] 5.12 Commit: "Phase 5 — cart, checkout, auth & admin UI (mock-backed)". **UI freeze: backend work may begin.**

## Phase 6 — Database & Laravel Models

- [x] 6.1 Scaffold `/backend`: latest Laravel, API-only (no Blade views/auth scaffolding); set JSON as default; add Pest. Link the project in **Laravel Herd** so it serves at a `.test` domain — local backend dev uses Herd, not Docker (CLAUDE.md §12).
- [x] 6.2 Configure `pgsql` connection to Supabase Postgres via env; create `backend/.env.example` per CLAUDE.md §11; verify `herd php artisan migrate` connects.
- [x] 6.3 Write migrations for `users` (with `supabase_id` uuid unique, `is_admin`), `categories`, `products`, `product_variants` — per the schema in CLAUDE.md §7.
- [x] 6.4 Write migrations for `orders`, `order_items`, `payments` (unique `reference`, `raw_payload` jsonb) with FK constraints and indexes (`slug`, `order_number`, `reference`).
- [x] 6.5 Create models with `$fillable`, relationships, and casts; add `OrderStatus` / `PaymentStatus` PHP enums.
- [x] 6.6 Write seeders mirroring the frontend mock data exactly (same categories, products, variants, pesewa prices) so the API swap is visually invisible.
- [x] 6.7 Add `GET /api/v1/health` (JSON + DB ping); confirm it responds at the Herd `.test` URL. Run migrations + seeds against Supabase (via `herd php artisan`); verify rows in a psql session.
- [x] 6.8 Commit: "Phase 6 — Laravel scaffold, schema, seeds".

## Phase 7 — Authentication

- [x] 7.1 Supabase dashboard: Email+Password enabled (confirmed via live sign-up). **Google OAuth deferred** — no code changes needed later, dashboard toggle only (CLAUDE.md §1).
- [x] 7.2 Frontend: create Supabase browser/server clients in `lib/supabase/`; implement session handling (`proxy.ts` token refresh — Next.js 16 renamed `middleware.ts` → `proxy.ts`).
- [x] 7.3 Wire the real login/register forms: sign-up, sign-in, sign-out, error states, `redirectTo` handling back to checkout. Google OAuth button deferred with the rest of 7.1.
- [x] 7.4 Add a `useUser` session hook and header account state (Sign in ↔ account menu with Orders + Sign out).
- [x] 7.5 Backend: implement `VerifySupabaseJwt` middleware — verify signature via Supabase's JWKS (ES256, this project's actual signing method — not the legacy HS256 shared secret; CLAUDE.md §6 updated), check `exp` + `aud`, alg allowlist per key; find-or-create local `users` row from `sub`/`email`; bind to request.
- [x] 7.6 Implement `EnsureAdmin` middleware (403 unless `is_admin`); registered as route-middleware aliases (`auth.supabase`, `admin`) in `bootstrap/app.php` — applied to real route groups in Phase 8.
- [x] 7.7 Pest tests: valid token passes, expired/tampered/missing/unknown-kid/wrong-alg token → 401, non-admin on admin route → 403, first-request user auto-provisioning, admin not de-escalated on repeat sign-in.
- [x] 7.8 Commit: "Phase 7 — Supabase auth end-to-end".

## Phase 8 — REST API

- [x] 8.1 Configure CORS for `FRONTEND_URL`; set up `/api/v1` route group and the success/error envelope conventions.
- [x] 8.2 Public catalog endpoints: `GET /categories`, `GET /products` (category filter, featured flag, pagination), `GET /products/{slug}` — with API Resources emitting camelCase exactly matching `frontend/src/types`.
- [x] 8.3 Customer order endpoints: `GET /orders` (own orders only), `GET /orders/{orderNumber}` (own-order authorization).
- [x] 8.4 `CheckoutService` + `POST /checkout`: FormRequest validation; re-validate items against DB (active, in stock, server prices); compute totals (flat GH₵20 delivery fee via `config('checkout.delivery_fee_pesewas')`); create order + items + pending payment in a transaction; return order payload (Paystack call added in Phase 9). Stock is checked but intentionally **not** decremented here — only the Phase 9 webhook decrements, per CLAUDE.md §9.
- [x] 8.5 Admin product endpoints: CRUD for products + variants (`/admin/products`…), including activate/deactivate; FormRequests for all writes.
- [x] 8.6 Admin inventory endpoint: `PATCH /admin/variants/{id}/stock`; admin orders: `GET /admin/orders` (filter by status), `PATCH /admin/orders/{orderNumber}` (status transitions validated against the enum via `OrderStatus::canTransitionTo()`).
- [x] 8.7 Pest feature tests: catalog shapes, own-order isolation, checkout rejects price tampering and overselling (409), admin CRUD happy paths + 403 for non-admins. 43 tests passing.
- [x] 8.8 Commit: "Phase 8 — REST API complete".

## Phase 9 — Paystack Integration

- [x] 9.1 Create Paystack account; get test keys; add `PAYSTACK_*` to backend env + `config/services.php`. (Test keys are from an existing personal Paystack account, reused for this project's test mode.)
- [x] 9.2 `PaystackService`: `initializeTransaction()` (amount in pesewas, GHS, email, generated unique reference, callback URL) and `verifyTransaction()` via Paystack API — typed responses, timeouts, logged failures. Network/connection failures are also normalized to `PaystackException` so callers only ever handle one exception type.
- [x] 9.3 Extend `POST /checkout` to initialize the transaction and return `{ authorizationUrl, reference, orderNumber }`; store the reference on the `payments` row. Paystack init happens inside the same DB transaction as order creation — a failed init rolls back the whole checkout (502).
- [x] 9.4 Webhook `POST /webhooks/paystack`: raw-body HMAC SHA512 check with `hash_equals` (401 on mismatch); excluded from auth middleware; rate-limited (`throttle:60,1`).
- [x] 9.5 On `charge.success`: re-verify via API, match amount+currency against our payment row, then in one transaction mark payment `success` + order `paid` + decrement variant stock; idempotent on replay (row-locked re-check inside the transaction); store `raw_payload`; 200 for all validly-signed events.
- [x] 9.6 Pest tests: bad signature → 401, success flow updates payment/order/stock, replayed webhook is a no-op (no double stock decrement), amount mismatch does not fulfill, `PaystackService` failure/timeout handling. 14 new tests, 57 total passing.
- [ ] 9.7 End-to-end test with Paystack test cards against the local Herd backend (tunnel the `.test` domain so Paystack can reach the webhook); configure the test webhook URL in the Paystack dashboard. **Deferred to the user** — requires real browser card entry; verified instead via CLI: real `PaystackService` calls against the live Paystack test API (init + verify, both succeeded), and a hand-signed webhook POST against the live Herd server + real Supabase DB (correctly refused to fulfill an unrecognized reference, proving re-verification isn't bypassable even with a valid signature). Tunnel + dashboard webhook URL registration still needed before a real card test.
- [x] 9.8 Commit: "Phase 9 — Paystack payments".

## Phase 10 — Connect Frontend to Backend

- [x] 10.1 Implement the real `lib/api/client.ts`: typed fetch wrapper on `NEXT_PUBLIC_API_URL`, attaches the Supabase access token, normalizes the error envelope into `ApiError`. Token is sourced via a client-mounted `AuthTokenProvider` reading the real Supabase session; public catalog reads work unauthenticated from Server Components.
- [x] 10.2 Swap `products.ts`/`categories.ts` internals from mocks to the client (signatures unchanged); verified landing, shop, and PDP render identically from live data (6 categories, 17 products — full parity with the mocks). Mock files not yet deleted — `admin.ts` still reads them until 10.5.
- [x] 10.3 Wire checkout: submit cart + delivery form to `POST /checkout`, redirect to `authorizationUrl`; built `/checkout/callback` that polls order status and forwards to the confirmation page; handles the still-pending/timeout case gracefully. **Decision:** dropped the mock's "free delivery over GH₵300" rule — flat GH₵20 always, matching the real backend exactly (no client/server mismatch).
- [x] 10.3b (added) Remember delivery details: last-used name/phone/address/city persist to `localStorage` (`stores/delivery.ts`) and pre-fill the checkout form next time — still fully editable, note stays blank per order.
- [x] 10.4 Wire order confirmation + order history to the live endpoints (own-account-scoped); clear the cart only after `/checkout/callback` confirms the order is no longer `pending`. Both pages gained an auth gate they didn't need before (mock store required no login).
- [x] 10.5 Wired admin screens to the real admin endpoints: `lib/api/admin.ts` now hits `/bo/products` (+ variants CRUD, stock), `/bo/orders`; `ProductFormDialog` creates/updates the product then diffs variant rows against separate variant endpoints (backend doesn't accept nested variants); `AdminOrders` dropped the mock `stores/orders.ts` (deleted, was dead everywhere else) for `getAdminOrders`/`updateOrderStatus`, with the status `Select` limited to the backend's actual `OrderStatus::allowedTransitions()` so the UI can't offer an illegal move. Added a "Sign out" control to the admin sidebar (`AdminShell`) — previously only reachable via "Back to store" first.
  - **Decision (access flow):** the frontend never knows `is_admin` from the JWT/session (by design, CLAUDE.md §6), so `AdminShell` (the layout, mounted once per admin session) probes `getAdminOrders({ perPage: 1 })` *before* rendering any admin chrome at all — sidebar, nav, page content — showing only a neutral "Loading admin…" text either way. A confirmed non-admin gets redirected to `/shop` having never seen anything admin-shaped (no "forbidden" messaging either, which would itself confirm "you have an account, just not an admin one"). The per-page 403 catches (already built into Products/Inventory/Orders) stay as defense-in-depth for the rare case `is_admin` is revoked mid-session. No new `/me` endpoint — reuses an existing admin-gated call purely as a yes/no probe.
  - **Decision (route naming, reversed once):** first pass kept the page at `/admin`; renamed the whole path afterward to `/bo` — both the frontend route (`frontend/src/app/admin` → `frontend/src/app/bo`) and the backend prefix (`routes/api.php`: `Route::prefix('admin')` → `Route::prefix('bo')`, plus the two admin Pest test files' URLs) — since renaming only one half still leaks "admin" via the other (devtools network tab shows the API prefix regardless of the page URL). No visible link to it anywhere in the storefront UI (typed-URL only, deliberately). Documented as obscurity, not a real gate, in CLAUDE.md §6 — there's no Laravel login form to brute-force in the first place (auth is Supabase-only); the actual protection is unchanged: `auth.supabase` (401) then `EnsureAdmin` (403).
  - Verified E2E: registered a second account via Gmail `+1` aliasing (confirmed distinct Supabase user + local `users` row, `is_admin` defaults false), confirmed the `/bo` redirect fires with zero admin-chrome flash; primary account flipped to `is_admin = true` via `tinker`, confirmed full dashboard access, product/variant/order CRUD, and sign-out. Backend: full Pest suite green post-rename (57/57).
- [x] 10.6 Full happy-path E2E on test keys: register → shop → cart → checkout (Paystack inline popup, not a redirect — see decision below) → webhook → confirmation → order in history → order visible/updatable in admin. **Webhook tunnel resolved:** routed around Valet's Host-header-based site resolution entirely — `php artisan serve --port=8001` (plain server, no vhost routing to confuse) tunneled via `ngrok http 8001`, registered as the Paystack test webhook URL. Verified twice: order `BS-2026-674792` and `BS-2026-186291` needed a manual replay (webhook URL wasn't saved yet in Paystack at checkout time — Paystack doesn't retry past events once you register a URL later, by design), each replayed faithfully by constructing a correctly HMAC-signed request and running it through the real `PaystackWebhookController` (genuine Paystack re-verification, not a raw DB edit — CLAUDE.md §9 makes the webhook the only place `paid` happens, and that held for the replay too). Order `BS-2026-658700` (3rd order) then confirmed the tunnel works live and unassisted: `artisan serve`'s own request log shows the real inbound `POST /api/v1/webhooks/paystack`, landing seconds before `payments.paid_at` — no manual replay involved.
  - **Decision (checkout presentation):** switched checkout from a full-page redirect to Paystack's hosted page to Paystack's Inline popup (`@paystack/inline-js`, `resumeTransaction(accessCode)`) — the storefront never fully unloads, avoiding the "did I leave Birchscents?" confusion of a bare redirect. `accessCode` was already being fetched from Paystack's Initialize Transaction response (`PaystackService`) but unused; now threaded through `CheckoutService`/`CheckoutController`. Zero change to the money-safety architecture: amount is still computed and sent to Paystack entirely server-side, and the popup's `onSuccess` still only triggers a client-side navigation to `/checkout/callback` to poll — it never marks anything paid itself. `onError` (e.g. an ad-blocker blocking the popup) falls back to the original full-page redirect via `authorizationUrl`, kept specifically for this. Package is dynamically imported inside the click handler, not statically — it touches `window` at module-eval time, which crashed Next's SSR pass on a static import.
  - **Bug found + fixed during this testing:** the cart wasn't clearing when a webhook confirmed payment *after* `/checkout/callback`'s ~30s polling window gave up (a real production risk too, not just local slowness). Fixed with a `pendingOrderNumber` stashed in `stores/cart.ts` (persisted, survives reloads) and a new invisible `CartOrderSync` component mounted at the `(store)` layout level — checks once per app load whether the stashed order is confirmed, and clears the cart if so, catching confirmations the callback page already missed.
- [x] 10.7 Error-path pass. `lib/api/client.ts`'s `apiFetch` now normalizes a genuinely unreachable backend into a friendly `ApiError` (`status: 0`) instead of leaking a raw browser `"Failed to fetch"` — every existing caller benefits with no changes needed there. Checkout (`checkout/page.tsx`) branches on status: 401 (expired session) toasts and redirects to `/login?redirectTo=/checkout` (cart untouched, client-side/localStorage survives); 409 (out-of-stock race) shows the backend's already-itemized message plus a "Review your cart" link. Added `frontend/src/app/(store)/error.tsx` and `frontend/src/app/global-error.tsx` — branded boundaries with a working "Try again" (`unstable_retry()`, new in Next 16.2 — confirmed via the bundled docs), so a backend outage shows on-brand messaging instead of Next's default error page; `(store)/error.tsx` keeps the header/footer visible since `error.tsx` doesn't replace its own segment's layout. Added `loading.tsx` skeletons (shop, category, PDP) matching the real grid/layout instead of a blank gap during navigation. `orders/page.tsx` now distinguishes "couldn't load your orders" (retry button) from "you haven't placed any orders yet" — previously a fetch failure silently collapsed into the empty-state copy. No backend changes — the existing 409 message was already good. **Verified live**: a real Herd restart mid-session produced exactly this path organically (`GET / 500` in the terminal, the new error boundary catching and logging the raw `ApiError`, clean recovery to `200` once Herd was back) — better confirmation than a staged test.
- [x] 10.8 No `/dev/styleguide` page exists in the repo (already gone/never built). Leftover mock imports removed: `mocks/products.ts`, `mocks/categories.ts`, `lib/api/mock-latency.ts` all had zero remaining imports and were deleted (commit `0146ef7`). Landed as several small per-feature commits rather than one wrap-up commit — see `b625bb8`, `0146ef7`, `81afee0`.

## Phase 11 — Testing & Performance Optimization

- [x] 11.1 Full backend suite green (64/64). Reviewed checkout totals coverage — already thorough (server-side pricing, delivery fee inclusion, multi-line sums, tampering ignored, 409s, rollback on Paystack failure). Found and fixed a real webhook edge case while reviewing: stock decrement had no floor at zero, so two orders paid for the last units of a low-stock variant could hit the Postgres `stock >= 0` CHECK constraint on the second one and roll back that entire transaction — including marking the payment successful, despite a genuine charge. Now locks the variant row and clamps at 0 (logs a warning for the oversold case), with a new regression test simulating exactly that race (commit `dcdd374`).
- [x] 11.2 `next build` clean (17 routes, zero warnings) and ESLint clean. Fixed `build` script too — it never had the `NODE_EXTRA_CA_CERTS` fix `dev` got, so it was silently failing against Herd's self-signed cert; confirmed safe for the eventual Docker build (Node just warns and continues if the cert path doesn't exist, doesn't fail). Dead code audit (forked, read-only) found and removed: `lib/checkout.ts` (old mock order-builder), `components/ui/badge.tsx` (unused shadcn primitive — `OrderStatusBadge` already covers the need), `lib/supabase/server.ts` (unused SSR Supabase client — every auth page ended up client-side). Bigger finding from the same audit: `lib/motion.ts` (a full Framer Motion preset library — CLAUDE.md §3 requires exactly this) was 100% unwired, and `prefers-reduced-motion` wasn't handled anywhere at all, not even by existing CSS hover effects. Wired it in rather than deleting it: scroll-reveal (`components/motion/Reveal.tsx`) on all 5 landing-page marketing sections, hover-lift on both product card variants, and a global CSS `prefers-reduced-motion` kill-switch in `globals.css` covering every animation in the app (existing CSS ones and future ones), plus per-component `useReducedMotion()` checks for the new Framer Motion ones specifically.
- [x] 11.3 Lighthouse on landing, shop, PDP (mobile, real DevTools throttling): Landing 99/100/100/100, Shop 94/100/100/100, PDP 95/100/100/90 (all clear Performance ≥ 90 / Accessibility ≥ 95 / SEO ≥ 95). Two real fixes: (1) font payload was the dominant LCP cost — `layout.tsx` was loading Archivo's and Inter's full variable weight ranges when only 4 weights are ever used app-wide, pinned to `["600","700","800"]`/`["400","500","600","700"]`; (2) shop pages had a real heading-order violation (h1 straight into `ProductCard`'s h3 product names, no h2 between) — added a screen-reader-only `<h2>Products</h2>` before the grid on `/shop` and `/shop/[category]` rather than changing `ProductCard`'s heading level globally, since that h3 is already correctly nested under a real h2 everywhere else it's used (landing's `FeaturedProducts`, PDP's related-products). Note: `--throttling-method=simulate` (Lighthouse's default/Lantern model) reported inflated LCP/Performance numbers on this machine that its own `lcp-breakdown-insight` diagnostic contradicted (~400ms accounted vs. a claimed 4-5s metric) — re-ran with `--throttling-method=devtools` (real emulated throttling) for the trustworthy numbers above. PDP's SEO 90 is the same meta-description Lighthouse/DOM-snapshot-timing artifact noted earlier — confirmed via repeated `curl` that the tag is present and correct in the raw served HTML, which is what real crawlers read.
- [x] 11.4 Accessibility sweep. Contrast check: brand green/destructive red/muted text all 5.17–5.44:1 against their backgrounds, clear of the 4.5:1 bar (no gold accent in this build — CLAUDE.md's palette language predates the actual leaf-green decision already baked into `globals.css`). Cart drawer, mobile nav sheet, and size popovers are all Radix-based and already had correct focus-trap/Escape/focus-return behavior for free; checkout form already had proper `Label`/`aria-invalid`/`aria-describedby`/`role="alert"`. Three real gaps found and fixed: (1) no "skip to main content" link anywhere, so keyboard users had to tab through the full header on every page — added one (visually hidden until focused) to `Header.tsx`, targeting a new `id="main-content"` on `<main>` in both the store and auth layouts; (2) the three size-picker widgets (`ProductCard`, `BuyPanel`, `CategorySizeToggle`) used `role="radio"`/`aria-checked` on plain buttons without implementing the roving-tabindex/arrow-key behavior that role promises assistive tech — switched to `aria-pressed` toggle buttons in a `role="group"` container, matching the actual interaction model; (3) the cart quantity stepper had no live region, so screen reader users pressing +/− never heard the updated quantity or line total — added `aria-live="polite"` to that row in `CartLineItem.tsx`.
- [ ] 11.5 Cross-device QA: iPhone-class Safari, Android Chrome, desktop Safari/Chrome/Firefox; fix layout/interaction defects.
- [x] 11.6 SEO/metadata final pass. Added `lib/site.ts` (`SITE_URL` from `NEXT_PUBLIC_SITE_URL`) as the single source for everything below. Root layout: `metadataBase` (fixes relative OG image resolution) plus default `openGraph`/`twitter` fields. New `app/opengraph-image.tsx` — a dynamic (`next/og`) brand card (wordmark + tagline on the actual ivory/charcoal/leaf-green tokens) used as the fallback everywhere until real product photography (11.7) enables per-product OG images. Favicon replaced with the real brand mark (multi-res `.ico` plus `icon.png`/`apple-icon.png`), swapped in for the placeholder from Phase 1. New `app/robots.ts` (disallows `/bo`, `/cart`, `/checkout`, `/account`, `/orders`, `/login`, `/register`) and `app/sitemap.ts` (landing, `/shop`, every category, every product — verified live against the running dev server, 22 URLs). The 7 client-only utility pages (cart, checkout, checkout/callback, orders, orders/confirmation/[orderNumber], account, account/profile) can't export `metadata` directly — Next's App Router restricts that to Server Components — so each got a thin server `layout.tsx` sibling instead (matching the existing `/bo` `robots: noindex` pattern) rather than restructuring any page; nested routes inherit `noindex` from their parent and just add a more specific title. Also removed 5 unused `create-next-app` scaffold SVGs from `public/` that a prior dead-code pass (11.2) missed since it only checked `src/`.
- [ ] 11.7 Content pass: real product photography and final copy replace placeholders; verify sensory, non-clichéd voice throughout.
- [ ] 11.8 Commit: "Phase 11 — QA & performance".

## Phase 12 — Docker, Coolify & Production Deployment

- [x] 12.1 Wrote `frontend/Dockerfile` (multi-stage: `deps` → `builder` → slim `node:22-alpine` `runner`, non-root `nextjs` user, port 3000) + `.dockerignore`; added `output: "standalone"` to `next.config.ts`. Real finding along the way: without Cache Components enabled, Next's default rendering model fully prerenders a page at `next build` time unless it uses a Request-time API — our landing/shop/category/PDP pages and `sitemap.ts` had none, so they'd have baked a permanent, build-time snapshot of products/stock into static HTML (stale until the next redeploy) and required the Docker build stage to reach the live API just to compile. Added `export const dynamic = "force-dynamic"` to all 4 catalog pages plus `sitemap.ts` so they render fresh per request instead. Also added `ARG`/`ENV` plumbing in the Dockerfile's builder stage for the 4 `NEXT_PUBLIC_*` vars — these get inlined into the JS bundle at build time, not read at container start, so Coolify must supply them as Docker build variables (not just runtime env vars) for 12.5. Verified: `docker build` succeeds (with real build-args mirroring prod config); `docker run` boots, stays up, and serves pages correctly — including graceful degradation via the existing `(store)/error.tsx` boundary when the backend is unreachable (expected in this isolated container test; full live-data verification is 12.6, against the real deployed backend).
- [x] 12.2 Wrote `backend/Dockerfile`: `composer:2` `vendor` stage (`composer install --no-dev --no-scripts`, keyed only on `composer.json`/`composer.lock` for cache-friendly layering) → final `php:8.4-fpm-alpine` stage with nginx installed alongside php-fpm in the same container (a `docker/entrypoint.sh` starts php-fpm in the background, then execs nginx in the foreground as the container's main process). Extensions added on top of the base image: `pdo_pgsql`/`pgsql` (needs `postgresql-dev`/`postgresql-libs` — installed as a `.build-deps` virtual package and purged after compiling, keeping only the runtime `postgresql-libs`), `bcmath`, `mbstring` (needs `oniguruma-dev`, same purge treatment), `opcache`. Confirmed the backend is a pure JSON API — `routes/web.php` is empty and `resources/js`/`resources/css` are unused `laravel new` scaffold leftovers — so no Node/Vite build stage was needed. `docker/nginx.conf` serves `public/` on port 8080 and proxies PHP through to php-fpm on `127.0.0.1:9000`. Entrypoint runs `config:cache` + `route:cache` only; migrations stay a separate Coolify deploy-step command, never on container boot. Two real build errors hit and fixed along the way: (1) `composer dump-autoload` failed in the final stage since `php:8.4-fpm-alpine` has no composer binary — the `vendor` stage's install doesn't need one either, so had to explicitly `COPY --from=composer:2 /usr/bin/composer /usr/bin/composer` into the final stage; (2) the initial `vendor` stage install runs before `app/`, `database/factories/`, `database/seeders/` exist in the build context, so its autoloader is a lazy PSR-4 mapping, not a real classmap — added a `composer dump-autoload --optimize --classmap-authoritative` after the full source is copied in to regenerate it properly. Verified end-to-end against the real Supabase Postgres connection (this one's fully testable locally, unlike the frontend — Supabase is reachable over the public internet): `docker run --env-file backend/.env`, then `GET /api/v1/health` returned `"database":"ok"` and `GET /api/v1/categories` returned real seeded data.
- [ ] 12.3 (Optional) root `docker-compose.yml` for container-parity smoke tests only — day-to-day local dev uses Herd + `npm run dev` (CLAUDE.md §12). Document the Herd setup and both dev servers in a short root README.
- [ ] 12.4 Provision the Hetzner VPS; install Coolify; point DNS (`birchscents.com` → frontend, `api.birchscents.com` → backend).
- [ ] 12.5 Create the two Coolify apps from the repo (base dirs `/frontend`, `/backend`), enter all production env vars, set health checks, set `php artisan migrate --force` as the backend deploy command.
- [ ] 12.6 Deploy backend, run migrations + production seed (categories/products), verify `/api/v1/health` over HTTPS; deploy frontend, verify TLS on both domains.
- [ ] 12.7 Switch to Paystack **live** keys in Coolify; set the live webhook URL in the Paystack dashboard; complete one real low-value live transaction end-to-end and confirm webhook fulfillment.
- [ ] 12.8 Production Supabase auth config: production site URL + redirect URLs, Google OAuth production credentials.
- [ ] 12.9 Launch checklist: `APP_DEBUG=false`, CORS locked to the production origin, admin account set, error monitoring at minimum via Coolify logs, database backup plan confirmed (Supabase automatic backups).
- [ ] 12.10 Enable auto-deploy on push to `main`. Tag `v1.0.0`. **Launch.** 🕯️

## Post-launch backlog (not MVP scope, revisit after v1.0.0)

- [ ] Server-side cart, synced across devices in real time for a signed-in shopper. Today the cart is client-only (Zustand + `localStorage`, CLAUDE.md §1) — deliberately zero-API-chatter for guest browsing, but it means the cart does not follow a logged-in user between devices/browsers at all. Real support needs a `cart_items` table keyed to the user, synced on every add/remove/qty-change (polling or websockets for the "real-time on another device" part) — a real architecture change, not a small tweak. Confirmed as a deliberate MVP gap, not a bug, when asked on 2026-07-13.
